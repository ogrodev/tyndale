---
title: Next.js セットアップ
description: Next.js で Tyndale をセットアップし、ロケールルーティング、サーバーレンダリング、静的生成に対応します。
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

## アダプターをインストール

```bash
npm install tyndale-next
```

## 設定

<Steps>

1. **Next.js config を更新**

   ```js
   // next.config.mjs
   import { withTyndaleConfig } from 'tyndale-next/config';

   export default withTyndaleConfig({
     // your existing Next.js config
   });
   ```

2. **middleware を追加**

   ```ts
   // middleware.ts
   export { tyndaleMiddleware as middleware } from 'tyndale-next/middleware';
   ```

   これは、ロケールの検出、URL ルーティング、ロケール cookie の設定を処理します。

3. **レイアウトにサーバープロバイダーを追加**

   ```tsx
   // app/[locale]/layout.tsx
   import { TyndaleServerProvider } from 'tyndale-next/server';

   export default async function RootLayout({
     children,
     params,
   }: {
     children: React.ReactNode;
     params: Promise<{ locale: string }>;
   }) {
     const { locale } = await params;

     return (
       <html lang={locale}>
         <body>
           <TyndaleServerProvider locale={locale}>
             {children}
           </TyndaleServerProvider>
         </body>
       </html>
     );
   }
   ```

</Steps>

## 仕組み

- `withTyndaleConfig` は、Next.js config にロケールパスのリライトを追加します
- middleware はユーザーの優先ロケールを検出し、それに応じてリダイレクトします
- `TyndaleServerProvider` はサーバーサイドで翻訳を読み込み、React context を介してすべてのコンポーネントで利用できるようにします