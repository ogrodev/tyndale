---
title: Next.js 设置
description: 在 Next.js 中设置 Tyndale，以实现语言环境路由、服务端渲染和静态生成。
---

import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';

## 安装适配器

```bash
npm install tyndale-next
```

## 配置

<Steps>

1. **更新你的 Next.js 配置**

   ```js
   // next.config.mjs
   import { withTyndaleConfig } from 'tyndale-next/config';

   export default withTyndaleConfig({
     // your existing Next.js config
   });
   ```

2. **添加中间件**

   ```ts
   // middleware.ts
   export { tyndaleMiddleware as middleware } from 'tyndale-next/middleware';
   ```

   这会处理语言环境检测、URL 路由以及设置语言环境 cookie。

3. **将服务端 Provider 添加到你的布局中**

   ```tsx
   // app/[locale]/layout.tsx
   import { TyndaleServerProvider } from 'tyndale-next/server';

   export default async function RootLayout({
     children,
     params,
   }: {
     children: React.ReactNode;
     params: Promise<{ locale: string }>;
   }) {
     const { locale } = await params;

     return (
       <html lang={locale}>
         <body>
           <TyndaleServerProvider locale={locale}>
             {children}
           </TyndaleServerProvider>
         </body>
       </html>
     );
   }
   ```

</Steps>

## 工作原理

- `withTyndaleConfig` 会在你的 Next.js 配置中添加语言环境路径重写规则
- 中间件会检测用户首选的语言环境并进行相应重定向
- `TyndaleServerProvider` 会在服务端加载翻译，并通过 React context 将其提供给所有组件